<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Required Permissions -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeWoman"
        tools:targetApi="34">

        <!-- ✅ Broadcast Receiver for Volume Button Detection -->
        <receiver
            android:name=".volumesafety.VolumeButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.VOLUME_CHANGED_ACTION" />
            </intent-filter>
        </receiver>

        <!-- ✅ Foreground Service Declaration -->
        <service
            android:name=".volumesafety.VolumeService"
            android:foregroundServiceType="location"
            android:enabled="true"
            android:exported="false"
            tools:ignore="ForegroundServicePermission" />

        <!-- ✅ Activities Declaration -->
        <activity android:name=".PoliceStation.ViewPoliceStationActivity" android:exported="false" />
        <activity android:name=".PoliceStation.NearestPoliceStationActivity" android:exported="false" />
        <activity android:name=".Hospital.NearestHospitalActivity" android:exported="false" />
        <activity android:name=".Hospital.ViewHospitalActivity" android:exported="false" />
        <activity android:name=".PoliceStationActivity" android:exported="false" />
        <activity android:name=".MapsActivity" android:exported="false" />
        <activity android:name=".NewsActivity" android:exported="false" />
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".SignUpActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".EmergencyContactActivity" android:exported="false" />

        <!-- ✅ Splash Screen Activity (Launcher) -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Secure API Key Reference for Google Maps (Uncomment when needed) -->
        <!--
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        -->

        <!-- ✅ Optional Fix for Legacy Dependencies -->
        <uses-library android:name="org.apache.http.legacy" android:required="false" />

    </application>
</manifest>
